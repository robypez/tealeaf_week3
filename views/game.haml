#header-inner.fixed-header-container
  .container
    .row
      .span12
        .heading-text
          %h1="Dealer <span>cards</span> (#{session[:dealer_hand_value]})"
    .row.animated.fadeInDown.delay
      - session[:dealer_hand].each do |card|
        .span2
          = view(card)
      - if session[:player_status] == :stay && session[:dealer_status] == :must_turn
        .span2
          %p
            %form{ :action => "/game/dealer/hit", :method => "post"}
              %button.reg-btn{:type => "submit"} Turn dealer card
#header-inner.fixed-header-container
  .container
    .row
      .span12
        .heading-text
          %h1="#{session[:name]} <span>cards</span> (#{session[:player_hand_value]})"
    .row.animated.fadeInDown.delay
      - session[:player_hand].each do |card|
        .span2
          = view(card)
      - unless @error || session[:player_status] == :stay || session[:player_hand_value] == 21
        .span2
          %form{ :action => "/game/player/stay", :method => "post"}
            %button.reg-btn{:type => "submit"} Stay
          %form{ :action => "/game/player/hit", :method => "post"}
            %button.reg-btn{:type => "submit"} hit